# образ ноды
FROM node:18-alpine

# рабочая директория контейнера
WORKDIR /app

# копируем package.json из директории проекта в корневую директорию контейнера
COPY package.json .

# команда установки зависимостей
RUN npm i

#копируем все что находится в директории проекта в контекст докера
COPY . ./

#прокидываем порт наружу - на нем запускается приложение
EXPOSE 8000

# команда запуска приложения, когда контейнер соберется
CMD ["npm", "start"]


